
// systems/UI.js

export function drawUI(ctx) {
    // We will build this out later!
    // For now, let's just print a message so you know it's connected.
    ctx.fillStyle = 'white';
    ctx.font = '16px Arial';
    ctx.fillText('UI System Online - Press any button on PS5 controller to connect', 20, 30);
}
